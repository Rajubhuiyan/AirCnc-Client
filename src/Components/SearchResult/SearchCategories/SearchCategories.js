

const SearchCategories = ({sd}) => {
    
    console.log(sd);

    return (
        <div style={{marginLeft: '20px'}}>
            <h1>{sd.name}</h1>
        </div>
    );
};

export default SearchCategories;